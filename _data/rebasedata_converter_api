REQUEST STRING [using curl]

$ curl -F files[]=@filename.db https://www.rebasedata.com/api/v1/convert?outputFormat=xlsx -o result.zip

README
    * Replace @filename.db with your db file to convert (including path)
        #! Don't remove @ symbol

    * Replace outputFormat=xlsx with your intended format

    * Replace result.zip with your output filename and format

